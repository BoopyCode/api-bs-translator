# API Bullshit-to-English Translator

Translates vague API error messages and corporate documentation into plain English with actionable fixes. Run: `python api_bs_translator.py "your error message here"` or just `python api_bs_translator.py` for interactive mode.